# CMS 原型系统设计系统

## 0. Research Log
- 方向来源：已批准的“编辑型内容门户”规格；采用精确留白、冷中性色和单一动作色，不复制任何品牌资产。
- 技术约束：Django 服务端模板与 Bootstrap 5；仅为移动导航使用少量原生 JavaScript。
- 验证目标：375px、768px、1280px；WCAG 2.2 AA；前台与原生 Django Admin 使用同一视觉语言。

## 1. 氛围与身份
安静、可信、易读的校园内容门户。识别点是编辑式节奏：标题、元数据、正文与操作依靠字号、留白和色阶形成秩序，不使用营销式巨型首屏、装饰渐变或品牌图像。界面不得出现 Apple 或其他公司的名称、标识、专有字体、图片或文案。

## 2. 色彩
颜色仅通过 `--cms-*` 令牌使用。蓝色只表达链接、焦点、选中与主要操作；状态同时使用文字和边框，不只依赖颜色。

| 角色 | 令牌 | 值 |
|---|---|---|
| 页面背景 | `--cms-bg` | `#f6f7f9` |
| 主表面 | `--cms-surface` | `#ffffff` |
| 次表面 | `--cms-surface-subtle` | `#f0f2f5` |
| 边框 | `--cms-border` | `#e4e7eb` |
| 强边框 | `--cms-border-strong` | `#c9ced6` |
| 主文字 | `--cms-text` | `#1f2329` |
| 次文字 | `--cms-text-secondary` | `#5b616e` |
| 动作 | `--cms-accent` | `#2563eb` |
| 动作强调 | `--cms-accent-strong` | `#1e50b8` |
| 动作浅底 | `--cms-accent-soft` | `#eaf0fd` |
| 危险 | `--cms-danger` | `#b42318` |
| 成功 | `--cms-success` | `#15803d` |
| 警告 | `--cms-warning` | `#9a6700` |

## 3. 排版
- 字体：`--cms-font-sans`，使用本机可用中文系统字体，不下载专有字体。
- 标题 600，正文 400，辅助信息 400/500；正文不小于 16px，Admin 辅助文字不小于 14px。
- 字阶：14/16/18/20/24/30px。文章阅读宽度 `--cms-read-max`，正文行高 1.8，目标 55 至 70 个中文字符。

## 4. 间距与布局
- 4px 基础单位；令牌为 4/8/12/16/20/24/32/48/64px。
- 页面最大宽度 `--cms-content-max: 70rem`，阅读栏 `--cms-read-max: 45rem`。
- 375px 单列和折叠菜单；768px 两列或窄侧栏；1280px 稳定侧栏与受控内容宽度。
- 长标题、长栏目名和无空格字符串必须换行，不得造成页面级横向滚动。

## 5. 组件
- **全局导航**：桌面/移动；default、hover、active、focus；当前页使用 `aria-current`。
- **页面标题区**：标题、说明、主要与次要入口；不使用营销 Hero。
- **栏目入口/文章行**：default、hover、active、focus、长文本、空描述。
- **元数据组**：栏目、日期、作者；使用次文字色，保持可读对比度。
- **搜索字段**：default、focus、filled、error、disabled；标签、帮助与错误语义关联。
- **按钮/链接**：primary、secondary、danger、disabled、pressed、focus；触控目标至少 44px。
- **条件摘要/分页**：无条件、单条件、组合条件、当前页、禁用和焦点。
- **空状态/消息**：无栏目、无文章、无结果、success、warning、error，均给出恢复路径。
- **Admin 表单/列表**：字段、帮助、错误、筛选、批量操作与删除确认；不隐藏原生功能。

## 6. 动效与交互
快速反馈 120ms，标准过渡 200ms，缓动 `cubic-bezier(0.2, 0, 0, 1)`。仅动画 `transform`、`opacity`、`filter`；按下轻微位移。`prefers-reduced-motion` 下移除非必要过渡。

## 7. 深度与表面
采用色阶与 1px 边框为主的混合策略。阴影仅用于确有浮层语义的移动菜单和消息，不使用卡片嵌套、光斑或装饰性渐变。圆角按 6/8/12px 分级。

## 8. 可访问性约束与债务
- WCAG 2.2 AA；正文 4.5:1、大字 3:1；完整键盘路径与可见 `:focus-visible`。
- 提供 skip link、语义地标、表单标签/帮助/错误关联、ARIA 当前页与菜单展开状态。
- 200% 放大仍可完成核心任务；375px 触控目标约 44px。
- 已接受债务：Django Admin 宽表格在 375px 可在表格容器内部滚动；本轮不增加打印样式。
