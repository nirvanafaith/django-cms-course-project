# 北京交通大学课程 CMS 设计系统

## 0. Research Log

- 视觉基准：2026-08-14 至 2026-08-15 通过真实浏览器检查 `https://www.bjtu.edu.cn/`，并用运行时样式与官网 CSS 核实品牌区、导航、内容分区、页脚和响应式规则。
- 素材基准：校名标识与五张 2480×801 首页图已本地化到 `cms_site/static/img/bjtu/`，来源和 SHA-256 记录于 `docs/12_北交大官网素材来源与许可.md`。
- 交互参考：轮播采用可中断的自动播放、上一张/下一张、暂停和方向键机制；任何用户操作都会暂停自动播放，`prefers-reduced-motion` 默认关闭自动播放。
- 技术约束：Django 5.2 服务端模板、原生 CSS 与 JavaScript；不使用 Bootstrap、外部图片、前端框架或额外动画库。
- 验证目标：1440×1000、768×1024、375×812；WCAG 2.2 AA；前台保持官网式信息密度，Admin 只做克制品牌化。

## 1. 氛围与身份

页面应呈现公立高校主站的清晰、正式和信息导向感。第一视口以本地校名标识、蓝色品牌区和全宽校园图片轮播建立身份；正文依靠明确分区标题、紧凑文章列表、日期和栏目元数据形成浏览节奏，不使用营销 Hero、渐变、装饰光斑、嵌套卡片或大面积圆角容器。

页面不显示“课程 CMS 原型”或“非官方网站”类标识；不复制官网 ICP 备案号或把官网版权声明冒充为本项目声明。

## 2. 色彩

颜色只通过 `--bjtu-*` 令牌使用。官网主蓝承担品牌区、主导航、链接和焦点；绿色与金色只用于日期、分区强调和状态，不能成为大面积背景。

| 角色 | 令牌 | 值 |
| --- | --- | --- |
| 品牌蓝 | `--bjtu-blue` | `#005bac` |
| 导航深蓝 | `--bjtu-nav-blue` | `#004e91` |
| 链接深蓝 | `--bjtu-blue-dark` | `#003b90` |
| 页脚蓝 | `--bjtu-footer` | `#065eb1` |
| 浅蓝底 | `--bjtu-blue-soft` | `#cce1fa` |
| 页面浅底 | `--bjtu-page` | `#f4f7fb` |
| 主表面 | `--bjtu-surface` | `#ffffff` |
| 主文字 | `--bjtu-text` | `#333333` |
| 次文字 | `--bjtu-muted` | `#666666` |
| 弱文字 | `--bjtu-subtle` | `#999999` |
| 边框 | `--bjtu-border` | `#d9d9d9` |
| 日期绿 | `--bjtu-green` | `#4ba509` |
| 强调金 | `--bjtu-gold` | `#eea200` |
| 错误红 | `--bjtu-danger` | `#b42318` |
| 成功绿 | `--bjtu-success` | `#15803d` |

## 3. 排版

- 字体：`--bjtu-font`，顺序为 `Microsoft YaHei`、SimSun、SimHei、`STHeiti Light`、STHeiti、Tahoma、Arial、Helvetica、sans-serif。
- 正文：14px/1.7；导航：16px/700；分区标题：20px/700；文章列表标题：16px/1.5；详情正文：16px/1.9。
- 字阶：12/14/16/18/20/24/30/36px。仅首页品牌图承担身份信号，不用视口缩放字号，不使用负字距。
- 详情阅读栏最大 45rem，中文正文目标每行约 32 至 42 个汉字。

## 4. 间距与布局

- 4px 基础单位；令牌为 4/8/12/16/20/24/32/40/48/60px。
- 内容最大宽度 `--bjtu-content-max: 1200px`；品牌区桌面高度约 90px；导航高度 46px。
- 首页轮播保持 2480:801 比例并占满可用宽度；主体分区使用全宽色带或无外框布局，不把页面章节包进浮动卡片。
- 992px 折叠导航；767px 双栏堆叠；480px 收紧横向间距，同时保持按钮和导航至少 44px 触控目标。
- 长标题、长栏目名和无空格字符串必须换行；页面不得出现横向滚动。Admin 宽表格只允许在表格容器内部横向滚动。

## 5. 组件与状态

- **品牌区**：本地 228×61 校名标识；default、focus；桌面左对齐，移动端缩放但保持比例。
- **主导航**：首页、栏目、文章查询和身份入口；“栏目”使用官网式一级导航下的八项 `dl` 子菜单；default、hover、active、focus、expanded；当前页使用 `aria-current`，移动按钮同步 `aria-expanded`。
- **首页轮播**：五张本地图片；active、inactive、paused、reduced-motion；提供上一张、下一张、暂停/继续、方向键和 `aria-live` 状态。
- **分区标题**：中文标题、英文辅助标题和右侧“更多”入口；default、focus；20px/700，使用蓝色或金色短边线表达层级；“更多”沿用官网深蓝文字和右箭头节奏。
- **头条与文章行**：default、hover、focus、长标题、无作者；标题、栏目、日期分层，不能整块做圆角卡片。
- **搜索字段**：default、focus、filled、error、disabled；标签、帮助和错误语义关联，错误不能只依赖颜色。
- **按钮与分页**：primary、secondary、disabled、pressed、focus、current；最小高度 44px。
- **栏目下拉菜单**：八个固定内容栏目；default、hover、focus、长文本；桌面端以官网 `dl` 弹层呈现，移动端在展开主导航后线性展示。每一项链接到带已选栏目条件的文章查询页。
- **空状态与消息**：无栏目、无文章、无结果、success、warning、error；均给出恢复路径。
- **Admin 品牌**：本地校名标识、蓝色页头、系统日志入口；保留 Django 原生列表、筛选、表单、批量操作、删除确认和响应式表格。

## 6. 动效与交互

- 快速反馈 120ms，标准过渡 200ms，轮播淡入 360ms；缓动 `cubic-bezier(0.2, 0, 0, 1)`。
- 只动画 `transform`、`opacity` 和 `filter`。空间切换可中断，按钮按下只做 1px 位移。
- 轮播自动播放间隔 6 秒；上一张、下一张、方向键或暂停按钮触发后停止自动播放，避免与用户争夺控制。
- `prefers-reduced-motion: reduce` 下关闭轮播自动播放和非必要过渡；导航开合保持即时可用。

## 7. 深度与表面

以前景图片、蓝色品牌带、浅色内容带和 1px 分隔线建立层次。阴影只用于确有浮层语义的移动导航与轮播控制，不使用装饰性渐变、光斑、玻璃模糊、嵌套卡片或超过 8px 的圆角。

## 8. 可访问性约束与债务

- WCAG 2.2 AA；正文 4.5:1、大字 3:1；完整键盘路径和可见 `:focus-visible`。
- 提供 skip link、语义地标、唯一 `h1`、有序标题层级、表单标签/帮助/错误关联、非空图片替代文本和轮播状态文本。
- 导航按钮、轮播控制、表单按钮和分页触控目标至少 44×44px；200% 放大仍可完成浏览、查询、登录和管理任务。
- 已接受债务：Django Admin 宽表格在 375px 可在表格容器内部滚动；本轮不增加打印样式，不为官网图片生成额外 AVIF/WebP 衍生文件，以保留授权来源文件的哈希可追溯性。
